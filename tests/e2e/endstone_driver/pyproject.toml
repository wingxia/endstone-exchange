[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "endstone-exchange-e2e-driver"
version = "0.1.0"
description = "Isolated Endstone player-command dispatcher for Exchange E2E tests"
requires-python = ">=3.10"

[project.entry-points."endstone"]
exchange_e2e_driver = "endstone_exchange_e2e_driver:ExchangeE2EDriver"
